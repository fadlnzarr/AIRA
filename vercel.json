{
    "buildCommand": "npm run build && npx prisma generate",
    "outputDirectory": "dist",
    "framework": "vite",
    "functions": {
        "api/*.ts": {
            "runtime": "nodejs20.x"
        }
    }
}